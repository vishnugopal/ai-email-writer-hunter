<p style="color: green"><%= notice %></p>

<%= render @cold_email %>

<div>
  <%= link_to "Back to cold emails", cold_emails_path %>
</div>

<%= turbo_stream_from "#{dom_id(@cold_email)}_progress" %>
